name: http_io
repository: https://github.com/dart-lang/http_io
description: HTTP Client and Server APIs.

publish_to: none

environment:
  sdk: ">=3.4.2 <4.0.0"

dev_dependencies:
  convert: ^3.1.1
  crypto: ^3.0.3
  lints: ^4.0.0
  test: ^1.25.6
