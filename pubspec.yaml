name: http_io
repository: https://github.com/dart-lang/http_io
description: HTTP Client and Server APIs.
version: 0.0.22

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  meta: ^1.16.0

dev_dependencies:
  convert: ^3.1.2
  crypto: ^3.0.6
  lints: ^5.1.0
  test: ^1.25.14